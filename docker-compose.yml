services:
  listless:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - listless-data:/app/data

  # --- Optional: uncomment to use PostgreSQL instead of SQLite ---
  # listless-pg:
  #   build: .
  #   env_file: .env
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     LISTLESS_DATABASE_URL: "postgresql+asyncpg://listless:listless@postgres:5432/listless"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #
  # postgres:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: listless
  #     POSTGRES_PASSWORD: listless
  #     POSTGRES_DB: listless
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U listless"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

volumes:
  listless-data:
  # pgdata:
